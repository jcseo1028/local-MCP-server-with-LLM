다음 코드를 리팩터링해주세요.

## 리팩터링 규칙

1. **가독성 향상**: 변수명·메서드명을 의미 있게 개선한다.
2. **중복 제거**: 반복되는 코드를 메서드/함수로 추출한다.
3. **구조 개선**: 단일 책임 원칙(SRP)을 적용한다. 너무 긴 메서드는 분리한다.
4. **현대적 문법**: 해당 언어의 최신 관용 표현(idiom)을 사용한다.
   - C#: pattern matching, collection expression, target-typed new 등
   - Python: f-string, walrus operator, dataclass 등
   - JavaScript/TypeScript: optional chaining, destructuring, nullish coalescing 등
5. **동작 보존**: 기존 기능과 동작을 변경하지 않는다 (리팩터링만).
6. **과도한 변경 금지**: 핵심 구조만 개선하고, 불필요한 리팩터링은 하지 않는다.
7. **들여쓰기를 원본과 동일하게 유지한다** — 원본이 탭이면 탭, 스페이스면 스페이스를 사용한다. 들여쓰기 크기도 원본과 동일하게 한다.
8. **요청 범위 준수**: 사용자가 특정 변경 범위를 명시하면 그 범위 밖의 코드는 수정하지 않는다.
   - 요청에 "만", "only", "~만 정리", "~만 추가" 등이 포함되면 해당 대상 외 변경 금지.
   - import/using 정리 요청 시 변수명·메서드명·로직을 절대 변경하지 않는다.
   - 불필요한 rename, 메서드 분리, 재배치를 하지 않는다.

## 출력 형식

{{files_context}}
위 "멀티 파일 수정 모드" 지시가 있으면 반드시 [FILE: 경로]...[/FILE] 형식으로만 출력한다.
수정이 없는 파일은 블록에 포함하지 않는다.

단건 모드(위 멀티 파일 지시 없음)일 때는 아래 구조로 답변한다:

### 변경 요약
리팩터링한 항목을 번호 목록으로 나열한다.

### 리팩터링된 코드
리팩터링된 **전체 코드**를 하나의 코드 블록 안에 반환한다.
반드시 ```{{language}} 로 시작하고 ``` 로 끝나는 코드 블록으로 감싼다.
원본 코드의 모든 줄을 포함해야 한다. 일부분만 출력하지 않는다.

{{single_code_section}}

한국어로 답변해주세요.
