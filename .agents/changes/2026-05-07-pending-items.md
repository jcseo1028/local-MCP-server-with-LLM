# 미결 항목 목록 (2026-05-07 기준)

> 금일 작성된 스펙 문서에서 "미결", "미구현", "향후" 로 분류된 항목을 통합 정리한다.
> 출처 스펙 파일을 함께 기재한다.

---

## A. Diff 기반 코드 편집 (출처: `2026-05-07-diff-based-code-edit-spec.md`)

| # | 항목 | 우선순위 | 비고 |
|---|------|----------|------|
| A-1 | **per-hunk accept/reject UI** — 각 hunk별 수락/거부 버튼 표시 | 중 | WPF 복잡도 상, 별도 스펙 필요 |
| A-2 | **서버 측 `hunks` 사전 계산** — MCP Server가 LLM 응답 파싱 시 diff 계산 후 `hunks[]`를 proposal에 포함 | 하 | VSIX 재계산 중복 제거 목적 |
| A-3 | **Inline diff 데코레이션** — VS 에디터에 추가/삭제 라인 하이라이트 표시 (IClassifier 기반) | 중 | 별도 스펙 필요 |

---

## B. 멀티 파일 편집 v2.3 이후 (출처: `2026-05-07-multi-file-edit-spec.md`)

| # | 항목 | 우선순위 | 비고 |
|---|------|----------|------|
| B-1 | **파일별 개별 승인/거부** — 체크박스 기반 선택 승인 | 중 | v2.3 목표 |
| B-2 | **VSIX 파일 선택 UI** — 파일 선택 다이얼로그 또는 트리뷰에서 수정 대상 파일 직접 선택 | 상 | v2.3 목표 |
| B-3 | **부분 적용 실패 시 롤백 정책** — 파일 A 성공, B 실패 시 A를 롤백할지 여부 정책 결정 | 중 | 의사결정 필요 |
| B-4 | **`[FILE:]` 블록 파싱 안정성 검증** — LLM 출력 형식 변동성에 대한 대안 파싱 방식 (JSON 구조화 출력 등) 검토 | 중 | 운영 후 실측 필요 |
| B-5 | **대용량 멀티 파일 토큰 초과 대응** — 파일별 청크 분할 전략 | 중 | 실측 후 설계 |

---

## C. v2.2 구현 완료 후 미연결 (출처: `2026-05-07-multi-file-v2.2-implementation.md`)

| # | 항목 | 우선순위 | 비고 |
|---|------|----------|------|
| C-1 | **`RunStartRequest.Files[]` 실제 전송** — VSIX `BtnSend_Click`에서 현재 열린 파일들을 수집하여 서버에 전달 | 상 | 현재 DTO는 준비됨, 수집 로직 미구현 |
| C-2 | **`ApplyResults[]` 서버 전송** — 멀티 파일 적용 결과를 파일별로 `ClientResultRequest.ApplyResults`에 채워서 전송 | 중 | 현재 단건 `Applied` 필드만 전송 |
| C-3 | **LLM 프롬프트에 `[FILE:]` 블록 형식 안내 추가** — 코드 수정 도구 프롬프트에 멀티 파일 응답 형식 지시 삽입 | 상 | 없으면 멀티 파일 모드 실질적으로 발동 안 됨 |

---

## 우선순위 요약

| 우선순위 | 항목 |
|----------|------|
| **즉시 (v2.2 완성)** | C-1 (파일 수집 전송), C-3 (프롬프트 형식 안내) |
| **단기 (v2.3)** | B-1 (파일별 승인), B-2 (파일 선택 UI), C-2 (ApplyResults 전송) |
| **중기** | A-1 (per-hunk UI), A-3 (Inline 데코레이션), B-3 (롤백 정책), B-4 (파싱 안정성) |
| **장기/선택** | A-2 (서버 사전 계산), B-5 (토큰 초과 대응) |
