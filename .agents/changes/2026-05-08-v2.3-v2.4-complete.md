# v2.3 / v2.4 구현 완료 (2026-05-08)

## v2.3 — 멀티 파일 편집 UX 강화

### C-1: RunStartRequest.Files[] 실제 전송
- **파일**: `SummaryToolWindowControl.cs`
- `CollectOpenFilesAsync()` — `IVsRunningDocumentTable`로 VS 열린 코드 파일 수집 (20개 확장자 허용)
- `SendMessageAsync()`에서 `startReq.Files` 설정

### C-2: ApplyResults[] 서버 전송
- **파일**: `SummaryToolWindowControl.cs`, `McpRestClient.cs`
- 멀티파일 적용 결과를 `ClientResultRequest.ApplyResults[]`에 담아 전송

### C-3: LLM 프롬프트 [FILE:] 형식 안내
- **파일**: `RunOrchestrator.cs`, `CodeToolBase.cs`, `prompts/fix_code_issues.prompt.md`, `prompts/refactor_current_code.prompt.md`, `prompts/add_comments.prompt.md`
- `BuildFilesContext()` 생성 + `files_context` 변수 프롬프트 삽입

### B-1: 파일별 개별 승인/거부 체크박스
- **파일**: `SummaryToolWindowControl.cs`, `ChatMessageViewModel.cs`
- `FileChangeInfo.IsSelected` 필드, Expander 헤더 CheckBox 바인딩

### B-2: 파일 선택 UI
- **파일**: `SummaryToolWindowControl.cs`
- `_btnFileSelect`, `_fileSelectBorder`, 체크리스트 패널 토글
- `BtnFileSelect_Click()`, `PopulateFileSelectPanelAsync()`, `UpdateFileSelectHint()`

### B-3: 멀티파일 Atomic Rollback
- **파일**: `SummaryToolWindowControl.cs`
- 적용 전 원본 메모리 백업 → 실패 시 `File.WriteAllText`로 전체 원복

### B-4: [FILE:] 파싱 폴백
- **파일**: `RunOrchestrator.cs`
- `ParseFileBlocksMarkdownHeading()` — `### path.ext`, `**path.ext**`, `// File: path` + 코드 펜스 패턴

---

## v2.4 — Per-Hunk UI + 서버 사전 계산

### A-1: Per-Hunk Accept/Reject UI
- **파일**: `SummaryToolWindowControl.cs`, `ChatMessageViewModel.cs`
- `HunkSelection` 클래스 (`HunkIndex`, `IsAccepted`, `Hunk`)
- `CodeChangeInfo.HunkSelections`, `FileChangeInfo.HunkSelections`
- diff 뷰 생성 시 자동 초기화 (기본 전체 수락)
- 단일/멀티파일 적용 로직 모두 필터링 적용
- `ApplyHunksToFileAsync()` — `fc.HunkSelections` 기반 선택 hunk만 적용

### A-3: Unified Diff 컬러 하이라이트
- **파일**: `SummaryToolWindowControl.cs`
- `CreateSingleFileDiffView()` 전면 교체 — unified diff 통합 뷰
- 삭제 라인: 빨간 배경(`-` 접두사), 추가 라인: 초록 배경(`+` 접두사)
- ±3 컨텍스트 라인, `@@ -x,y +x,z @@` hunk 헤더
- `MakeDiffLineTb()`, `SplitDisplayLines()` 헬퍼 추가

### A-2: 서버 측 Hunks 사전 계산
- **파일(서버)**: `McpServer/DiffEngine.cs` (신규), `McpServer/RunModels.cs`, `McpServer/RunOrchestrator.cs`, `McpServer/McpEndpoints.cs`
- **파일(VSIX)**: `Services/McpRestClient.cs`, `Services/ChatMessageViewModel.cs`, `ToolWindows/SummaryToolWindowControl.cs`
- `DiffEngine.cs` — LCS diff 서버 포팅 (`DiffEngine.Compute()`)
- `DiffHunkInfo` 클래스 + `FileChange.Hunks` 필드
- `BuildFileChange()`에서 hunks 사전 계산
- `McpEndpoints.BuildRunSnapshot()`에 `changes[]` + `hunks[]` 직렬화 **(기존 멀티파일 Changes 누락 버그도 수정)**
- `DiffHunkDto` + `FileChangeDto.Hunks` DTO 추가
- VSIX: precomputed hunks → `HunkSelections` 매핑, `LineDiffEngine.Compute()` 재계산 생략
- **버그 수정**: 멀티파일 `fakeMsg.HunkSelections` → `fc.HunkSelections` 동기화 누락 수정

### B-5: 대용량 파일 토큰 초과 대응
- **파일**: `McpServer/RunOrchestrator.cs`
- `BuildFilesContext()`에 파일당 8,000자 / 전체 32,000자 제한
- 초과 시 비율 분배 절단 + 프롬프트에 `// ... (이하 생략)` 표시

---

## 영향 계약 (contracts.md 갱신)
- §11b `ChatRunSnapshot.proposal.changes[].hunks[]` 추가 (A-2)
- §11a `ChatRunStartRequest.files[].code` B-5 토큰 제한 주석 추가

## 영향 모듈 (modules.md 갱신)
- MCP Server: `DiffEngine.cs`, `DiffHunkInfo`, B-4 폴백 파싱, B-5 절단 설명 추가
- VS Extension: A-1/A-2/A-3/B-1/B-2/B-3 기능 설명, 구현 파일 목록 갱신
